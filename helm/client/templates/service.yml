apiVersion: v1
kind: Service
metadata:
  name: client
spec:
  type: {{ .Values.service.type }}
  selector:
    app: client
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
